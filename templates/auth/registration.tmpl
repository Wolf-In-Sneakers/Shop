{% extends "base.tmpl" %}

{% block content %}
    <form action="" method="POST" class="container regist flex-center">
        <input type="text" name="name" class="input" placeholder="Name" required>
        <input type="text" name="login" class="input" placeholder="Login" required>
        <input type="password" name="password" class="input" placeholder="Password" required>
        <input type="password" name="check_password" class="input" placeholder="Password" required>
        <button type="submit" name="add_registration" class="btn">Sign up</button>
        <p>Already have an account? <a href="/account/login" class="btn login">Sign in</a>
        </p>
    </form>
{% endblock %}